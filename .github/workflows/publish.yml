name: Publish Package ⚡️

on:
  release:
    types: [created] # 只有在你发布 Release 时才会触发，避免浪费额度

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions: # 显式声明权限，防止 Settings 里的开关没开
      contents: read
      packages: write 
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: 配置身份验证 🔑
        run: |
          echo "@Nakanomk:registry=https://npm.pkg.github.com" > .npmrc
          echo "//npm.pkg.github.com/:_authToken=${{ secrets.GITHUB_TOKEN }}" >> .npmrc

      - name: 安装依赖并发布 🚀
        run: |
          bun install
          bun publish
